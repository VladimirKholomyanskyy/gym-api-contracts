type: object
required:
  - id
  - profileId
  - exerciseId
  - workoutSessionId
  - setNumber
  - repsCompleted
  - weightUsed
  - loggedAt
properties:
  id:
    type: string
    format: uuid
    description: Unique identifier for the log entry.
  profileId:
    type: string
    format: uuid
    description: Identifier for profile
  workoutSessionId:
    type: string
    format: uuid
    description: Identifier for workout sessionv
  exerciseId:
    type: string
    format: uuid
    description: Identifier for the exercise being logged.
  setNumber:
    type: integer
    format: int32
    description: Number of the set being logged.
  repsCompleted:
    type: integer
    format: int32
    description: Number of repetitions completed in the set.
  weightUsed:
    type: integer
    format: int32
    description: Weight used for the exercise.
  loggedAt:
    type: string
    format: date-time
    description: Timestamp when the exercise log was created.
