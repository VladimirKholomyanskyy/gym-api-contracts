type: object
properties:
  sex:
    $ref: "../schemas/Sex.yaml"
  weight:
    type: number
    format: double
    description: Users weight.
  height:
    type: number
    format: double
    description: Users height.
  birthday:
    type: string
    format: date
  avatarUrl:
    type: string
    description: Avatar image url
